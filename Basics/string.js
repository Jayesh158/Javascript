let gameName = "Clash-Of-Clans";
let gameLevel = 108;
console.log(`I prefer to play ${gameName} and my current level is ${gameLevel} `)

let newGameName = new String("Free Fire") 

console.log(gameName.length)  // Shows the length of the string
console.log(gameName.anchor("game")) // Creates an anchor tag with the string
console.log(gameName.at(7))  // Returns the character on the specified index
console.log(newGameName.big()) // Converts the string to big text
console.log(gameName.charAt(4)) // Returns the character at the specified index
console.log(gameName.concat(" Is easy to play")) // Adds a string to the end of the original string
console.log(newGameName.fontcolor("red")) // Changes the font color of the string
console.log(newGameName.fontsize(24)) // Changes the font size of the string
console.log(gameName.includes("Clans")) // Checks if the string contains the specified substring
console.log(gameName.lastIndexOf("Clans")) // Returns the index of the last occurrence of the specified substring
console.log(gameName.link("https://www.clashofclans.com/")) // Creates a hyperlink with the string
console.log(gameName.indexOf("Clans")) // Returns the index of the first occurrence of the specified substring
console.log(gameName.match("Clans")) // Returns an array of matches for the specified substring
console.log(gameName.repeat(2)) // Repeats the string the specified number of times
console.log(gameName.replace("Clash", "Clash Royale")) // Replaces the specified substring with another string
console.log(gameName.search("Clans")) // Searches for the specified substring and returns its index
console.log(gameName.slice(0, 5)) // Extracts a section of the string
console.log(gameName.split("-")) // Splits the string into an array using the specified separator
console.log(gameName.toLowerCase()) // Converts the string to lowercase
console.log(gameName.toUpperCase()) // Converts the string to uppercase
console.log(gameName.trim()) // Removes whitespace from both ends of the string;
console.log(gameName.valueOf()) // Returns the primitive value of the string
console.log(gameName.startsWith("Clash")) // Checks if the string starts with the specified substring
console.log(gameName.endsWith("Clans")) // Checks if the string ends with the specified substring
console.log(gameName.padStart(20, "*")) // Pads the start of the string with the specified character
console.log(gameName.padEnd(20, "*")) // Pads the end of the string with
console.log();

