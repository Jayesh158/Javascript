const score = 400
//console.log(score);
/*
const balance = new Number(100)
console.log(balance); 

console.log(balance.toString().length); // Convert to string and get length

console.log(balance.toFixed(1)); // Convert to string with 1 decimal place

const otherNumber = 123.8966

console.log(otherNumber.toPrecision(4)); // Convert to string with 4 significant digits
  
const hundreds = 1000000
console.log(hundreds.toLocaleString('en-IN')); // Convert to string with locale formatting

*/

// +++++++++++++ Maths +++++++++++++++++++++++++++++

console.log(Math);
console.log(Math.abs(-4)); // Absolute value
console.log(Math.sqrt(121)); // Square root
console.log(Math.round(4.6)); // Round to nearest integer
console.log(Math.pow(2, 6)); // Power (2 raised to the power of
console.log(Math.ceil(4.2)); // Round up to nearest integer
console.log(Math.floor(4.9)); // Round down to nearest integer
console.log(Math.min(4, 3, 6, 8)); // Find minimum value
console.log(Math.max(4, 3, 6, 8)); // Find maximum value

console.log(Math.random()); // Generate a random number between 0 and 1

console.log(Math.floor((Math.random(1)*6) + 1)); 

